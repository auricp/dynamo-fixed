{
  "name": "dynamodb-mcp-server",
  "version": "0.1.0",
  "description": "DynamoDB MCP server for managing DynamoDB resources",
  "type": "module",
  "bin": {
    "mcp-server-dynamodb": "build/index.js"
  },
  "scripts": {
    "build": "shx rm -rf dist && tsc && shx chmod +x dist/*.js",
    "watch": "tsc --watch",
    "start": "node dist/index.js"
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.723.0",
    "@aws-sdk/util-dynamodb": "^3.723.0",
    "@modelcontextprotocol/sdk": "^1.10.2",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "ts-node": "^10.9.2",
    "zod": "^3.25.76",
    "@smithy/shared-ini-file-loader": "^1.0.0"

  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/node": "^20.14.0",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}
